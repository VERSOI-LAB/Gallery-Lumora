// Phase 1 has no artist authentication yet, so the studio screens act on behalf
// of a single hardcoded artist. Replace with the signed-in artist's id/slug once
// Supabase Auth is wired up in Phase 2.
export const CURRENT_ARTIST_SLUG = "han-soi";
